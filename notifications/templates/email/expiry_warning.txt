{% if check_key == "ssl" %}SSL-сертификат истекает{% else %}Домен истекает{% endif %}

У сайта {{ site.name }} ({{ site.domain }}) {{ what }} истекает {{ expires_on|date:"d.m.Y" }}.
Осталось дней: {{ days_left }}.

{% if check_key == "ssl" %}Проверьте, что автопродление сертификата настроено и работает.
Если сертификат истечёт, браузеры начнут показывать посетителям
предупреждение безопасности.{% else %}Продлите домен у регистратора заранее. Если домен истечёт,
сайт станет недоступен.{% endif %}

Открыть в панели: {{ site_url }}/sites/{{ site.id }}/

— SEOdyssey · {{ site_url }}
